extends layout.pug
include mixins/_setsRow

block content
    h1 Sets
    table.ui.small.striped.table
        thead
            tr
            th(colspan=2) Name
            th Cards
            th Release Date
            th Languages
        tbody
            each set in data
                +tableRow(set)
        tfoot
            tr
            th(colspan='4')