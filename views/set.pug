extends layout.pug
include mixins/_montageCardsRow

block content
    each set in data
        h1.ui.header #{ set.name }
        .ui.centered.link.cards
            each card in set.cards
                +tableRow(card)
